//uses the type PImage to load a file fromn the disk
//display using the image(img,x,y)
//or using the image(img,x,y,widht,height);
// uses imageMode() the same as rectMode() to determine how this is displayed

PImage img;

void myImage(){
  
 img = loadImage("something.jpg"); //file stored in the data folder
}